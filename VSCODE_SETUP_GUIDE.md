# VSCode 开发环境搭建与运行指南

## 📋 前置要求

在开始之前，请确保您的电脑已安装以下软件：

1. **Git** - [下载地址](https://git-scm.com/downloads)
2. **Node.js** (建议版本 18.x 或更高) - [下载地址](https://nodejs.org/)
3. **VSCode** - [下载地址](https://code.visualstudio.com/)

## 🔧 推荐的VSCode扩展

为了获得最佳的开发体验，建议安装以下VSCode扩展：

- **ESLint** - 代码检查工具
- **Prettier - Code formatter** - 代码格式化工具
- **ES7+ React/Redux/React-Native snippets** - React代码片段
- **Tailwind CSS IntelliSense** - Tailwind CSS智能提示
- **TypeScript Vue Plugin (Volar)** - TypeScript支持
- **GitLens** - Git增强工具

## 📥 拉取最新代码

### 方法一：使用命令行（推荐）

1. **打开终端**
   - Windows: 使用 Git Bash 或 PowerShell
   - Mac/Linux: 使用 Terminal

2. **克隆仓库**（首次获取代码）
   ```bash
   # 导航到您想要存放项目的文件夹
   cd ~/Documents/Projects  # 示例路径，请根据实际情况修改
   
   # 克隆仓库
   git clone [仓库URL]
   
   # 进入项目目录
   cd [项目名称]
   ```

3. **拉取最新代码**（已有本地代码）
   ```bash
   # 查看当前分支
   git branch
   
   # 切换到主分支
   git checkout main  # 或者 master，取决于您的默认分支名称
   
   # 拉取最新代码
   git pull origin main
   
   # 如果需要切换到其他分支
   git checkout [分支名称]
   git pull origin [分支名称]
   ```

### 方法二：使用VSCode内置Git功能

1. **打开VSCode**
   
2. **打开项目文件夹**
   - 点击 `文件` → `打开文件夹`
   - 选择项目所在的文件夹

3. **使用源代码管理面板**
   - 点击左侧活动栏的源代码管理图标（第三个图标）
   - 点击 `...` 菜单
   - 选择 `拉取` 或 `Pull`

4. **使用命令面板**
   - 按 `Ctrl+Shift+P` (Windows/Linux) 或 `Cmd+Shift+P` (Mac)
   - 输入 `Git: Pull` 并回车

## 🚀 安装依赖与运行项目

### 1. 安装项目依赖

在VSCode中打开终端（`Ctrl+` ` 或 `查看` → `终端`），运行：

```bash
# 安装所有依赖包
npm install

# 或者使用 yarn（如果您使用yarn）
yarn install

# 或者使用 pnpm（如果您使用pnpm）
pnpm install
```

### 2. 启动开发服务器

```bash
# 启动开发服务器
npm run dev

# 或者使用 yarn
yarn dev

# 或者使用 pnpm
pnpm dev
```

项目启动后，终端会显示访问地址，通常是：
- 本地访问：http://localhost:5173
- 网络访问：http://[您的IP]:5173

### 3. 其他常用命令

```bash
# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 运行代码检查
npm run lint

# 类型检查
npm run typecheck
```

## 🐛 在VSCode中调试

### 配置调试环境

1. **创建调试配置文件**
   - 点击左侧活动栏的运行和调试图标（第四个图标）
   - 点击 `创建 launch.json 文件`
   - 选择 `Chrome` 作为环境

2. **配置launch.json**
   
   在项目根目录创建 `.vscode/launch.json` 文件：

   ```json
   {
     "version": "0.2.0",
     "configurations": [
       {
         "type": "chrome",
         "request": "launch",
         "name": "Launch Chrome against localhost",
         "url": "http://localhost:5173",
         "webRoot": "${workspaceFolder}/src",
         "sourceMaps": true,
         "runtimeArgs": ["--auto-open-devtools-for-tabs"]
       }
     ]
   }
   ```

### 开始调试

1. **设置断点**
   - 在代码编辑器中，点击行号左侧设置断点（红点）

2. **启动调试**
   - 确保开发服务器正在运行（`npm run dev`）
   - 按 `F5` 或点击调试面板的启动按钮
   - Chrome浏览器会自动打开并加载应用

3. **使用调试控制台**
   - 使用调试工具栏控制程序执行（继续、单步跳过、单步调试等）
   - 在调试控制台查看变量值和执行表达式

## 💡 开发技巧

### 使用VSCode集成终端

- **打开多个终端**：点击终端面板右上角的 `+` 按钮
- **分屏终端**：点击终端面板右上角的分屏图标
- **快速导航**：使用 `Ctrl+PageUp/PageDown` 切换终端

### 代码导航快捷键

- `Ctrl+P`：快速打开文件
- `Ctrl+Shift+P`：打开命令面板
- `Ctrl+Shift+F`：全局搜索
- `F12`：跳转到定义
- `Alt+F12`：查看定义（预览）
- `Shift+F12`：查看所有引用
- `Ctrl+点击`：跳转到定义

### Git操作快捷方式

- `Ctrl+Shift+G`：打开源代码管理面板
- `Ctrl+Enter`：提交更改（在源代码管理面板中）

## 🔄 更新代码的最佳实践

1. **提交本地更改前先拉取**
   ```bash
   git add .
   git commit -m "您的提交信息"
   git pull origin main
   git push origin main
   ```

2. **处理冲突**
   - VSCode会高亮显示冲突区域
   - 选择保留哪个版本或手动编辑
   - 解决后标记为已解决

3. **使用分支开发**
   ```bash
   # 创建并切换到新分支
   git checkout -b feature/您的功能名称
   
   # 开发完成后推送分支
   git push origin feature/您的功能名称
   ```

## 📚 项目结构说明

```
├── src/                # 源代码目录
│   ├── components/     # React组件
│   ├── contexts/       # React Context
│   ├── lib/           # 工具函数和常量
│   ├── pages/         # 页面组件
│   ├── App.tsx        # 主应用组件
│   ├── main.tsx       # 应用入口
│   └── index.css      # 全局样式
├── public/            # 静态资源
├── package.json       # 项目配置和依赖
├── vite.config.ts     # Vite配置
├── tsconfig.json      # TypeScript配置
└── tailwind.config.js # Tailwind CSS配置
```

## ❓ 常见问题解决

### 1. npm install失败
```bash
# 清除缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 2. 端口被占用
```bash
# 查找占用5173端口的进程
# Windows
netstat -ano | findstr :5173

# Mac/Linux
lsof -i :5173

# 终止进程或修改vite.config.ts中的端口配置
```

### 3. TypeScript错误
```bash
# 重新生成类型定义
npm run typecheck

# 重启TypeScript服务
# 在VSCode中：Ctrl+Shift+P → TypeScript: Restart TS Server
```

## 📞 获取帮助

如果遇到问题，可以通过以下方式获取帮助：

1. 查看项目的 README.md 文件
2. 查看项目的 Issue 页面
3. 联系项目维护者
4. 查阅相关技术文档：
   - [Vite文档](https://vitejs.dev/)
   - [React文档](https://react.dev/)
   - [TypeScript文档](https://www.typescriptlang.org/)

---

最后更新：2024年11月