# CSV 数据导入 SQL Server - 完成总结

## 📋 导入结果

✅ **所有数据导入成功！**

### 导入统计
- **总表数**: 11 个表
- **总行数**: 3013 行
- **导入时间**: 2026-01-29 17:37
- **耗时**: 约 1 分钟

### 各表导入详情

| 序号 | 表名 | 行数 | 状态 | 备注 |
|------|------|------|------|------|
| 1 | departments | 14 | ✅ | 部门基础数据，使用 IDENTITY_INSERT |
| 2 | factories | 9 | ✅ | 工厂基础数据，使用 IDENTITY_INSERT |
| 3 | task_types | 8 | ✅ | 任务类型基础数据，使用 IDENTITY_INSERT |
| 4 | resource_task_types | 11 | ✅ | 资源任务类型，使用 IDENTITY_INSERT |
| 5 | skills | 104 | ✅ | 技能数据，禁用约束后导入 |
| 6 | competency_definitions | 39 | ✅ | 能力定义，使用 IDENTITY_INSERT |
| 7 | employees | 18 | ✅ | 员工数据 |
| 8 | competency_assessments | 375 | ✅ | 能力评估数据（最大表之一） |
| 9 | tasks | 15 | ✅ | 任务数据 |
| 10 | resource_planning_tasks | 2411 | ✅ | 资源规划任务（最大表），使用 IDENTITY_INSERT |
| 11 | schedule_change_notifications | 9 | ✅ | 计划变更通知 |
| **总计** | **11 表** | **3013** | **✅ 100%** | **全部成功** |

---

## 🛠️ 解决的问题

### 1. 日期时间格式转换
**问题**: PostgreSQL 格式 `2025-11-25 02:51:25.096953+00` 无法直接导入 SQL Server

**解决方案**:
- 使用正则表达式提取 `YYYY-MM-DD HH:MM:SS` 格式
- 忽略毫秒和时区信息
- 修改 `convert_value()` 函数，添加日期处理逻辑

### 2. IDENTITY 列识别
**问题**: 不是所有表都有 IDENTITY 列，错误配置导致导入失败

**解决方案**:
- 编写检查脚本 `check_identity_columns.py` 识别所有 IDENTITY 列
- 发现只有 7 个表有 IDENTITY 列（不是所有表）
- 准确配置 `identity_insert` 选项

**有 IDENTITY 的表**:
- departments.id
- factories.id
- task_types.id
- resource_task_types.id
- skills.id
- competency_definitions.id
- resource_planning_tasks.id

**没有 IDENTITY 的表**:
- employees (使用 UUID)
- competency_assessments (使用 UUID)
- tasks (使用 UUID)
- schedule_change_notifications (使用 UUID)

### 3. UNIQUE 约束冲突
**问题**: `skills` 表的 `skill_code` 字段有唯一约束，但 CSV 中有多个 NULL 值

**解决方案**:
- 临时删除唯一约束 `uq_skills_code`
- 导入完成后需手动重建（使用过滤索引允许 NULL）

### 4. 字段跳过配置错误
**问题**: 
- `tasks` 表的 `task_type` 和 `task_location` 字段被错误跳过
- 这两个字段在 SQL Server 中是 NOT NULL，导致导入失败

**解决方案**:
- 检查 CSV 文件实际包含这些字段
- 从 `skip_columns` 中移除，只跳过真正不需要的字段
- 最终 skip_columns: `['days_count', 'source', 'is_cross_factory', 'request_factory']`

### 5. 依赖索引阻止列修改
**问题**: `tasks.task_type` 列有依赖索引 `idx_tasks_type`，无法直接修改列定义

**解决方案**:
- 删除依赖索引 `idx_tasks_type`
- 允许列定义修改
- 导入后需手动重建索引

### 6. Windows 命令行 UTF-8 编码
**问题**: Windows PowerShell 默认使用 CP1252 编码，无法显示中文

**解决方案**:
```python
if sys.platform == 'win32':
    sys.stdout.reconfigure(encoding='utf-8')
    sys.stderr.reconfigure(encoding='utf-8')
```

---

## 📝 导入流程

### 1. 前置修复（必需）
```bash
# 运行前置修复脚本
python run_pre_import_fix.py
```

**作用**:
- 删除 `skills` 表的唯一约束 `uq_skills_code`
- 删除 `tasks` 表的索引 `idx_tasks_type`
- 修改 `tasks.task_type` 列允许 NULL（虽然最终没用到）

### 2. 批量导入
```bash
# 运行自动导入脚本
python import_csv_to_sqlserver.py
```

**导入顺序**（遵循外键依赖）:
1. 第一批（基础表）: departments, factories, task_types, resource_task_types, skills, competency_definitions
2. 第二批（依赖第一批）: employees
3. 第三批（依赖第二批）: competency_assessments, tasks, resource_planning_tasks, schedule_change_notifications

### 3. 后置恢复（建议）
```bash
# 运行后置恢复脚本
python run_post_import_fix.py  # 需要创建
```

**作用**（待执行）:
- 重建 `skills` 表的唯一索引（使用过滤索引允许 NULL）
- 重建 `tasks` 表的索引 `idx_tasks_type`
- 验证数据完整性

---

## 🔧 关键配置

### SQL Server 连接
```python
SQL_SERVER_CONFIG = {
    'server': '10.88.43.154',
    'database': 'DCCT_BPS_Debug',
    'username': 'TEST',
    'password': '123456',
    'driver': '{ODBC Driver 17 for SQL Server}'
}
```

### IDENTITY_INSERT 处理
- 自动为 7 个表开启 IDENTITY_INSERT
- 导入原始 ID 值，保持与 Supabase 一致
- 导入后自动关闭 IDENTITY_INSERT

### 字段跳过
- **employees**: 跳过 `position`, `auth_user_id` (SQL Server 中不存在)
- **competency_assessments**: 跳过 `gap` (计算列)
- **tasks**: 跳过 `days_count`, `source`, `is_cross_factory`, `request_factory`

### 约束禁用
- **skills**: 临时禁用外键约束，导入后重新启用
- 使用 `disable_constraints: True` 配置

---

## 📊 数据验证

### 自动验证结果
```
表名                                  预期行数         实际行数         状态
----------------------------------------------------------------------
departments                         14           14           ✅
factories                           9            9            ✅
task_types                          8            8            ✅
resource_task_types                 11           11           ✅
skills                              104          104          ✅
competency_definitions              39           39           ✅
employees                           18           18           ✅
competency_assessments              375          375          ✅
tasks                               15           15           ✅
resource_planning_tasks             2411         2411         ✅
schedule_change_notifications       9            9            ✅
----------------------------------------------------------------------
总计                                  3013         3013         ✅
```

### 手动验证建议
```sql
-- 验证外键完整性
SELECT 'employees' AS table_name, COUNT(*) AS rows 
FROM employees WHERE department_id NOT IN (SELECT id FROM departments);

SELECT 'competency_assessments' AS table_name, COUNT(*) AS rows
FROM competency_assessments 
WHERE employee_id NOT IN (SELECT id FROM employees)
   OR skill_id NOT IN (SELECT id FROM skills);

SELECT 'tasks' AS table_name, COUNT(*) AS rows
FROM tasks WHERE assigned_employee_id NOT IN (SELECT id FROM employees);

-- 应该都返回 0 行
```

---

## ⚠️ 待完成任务

### 1. 重建约束和索引
需要运行 `POST_IMPORT_FIX.sql`:
```sql
-- 重建 skills 的唯一索引（允许 NULL）
CREATE UNIQUE NONCLUSTERED INDEX uq_skills_code 
ON dbo.skills(skill_code) 
WHERE skill_code IS NOT NULL;

-- 重建 tasks 的索引
CREATE INDEX idx_tasks_type ON dbo.tasks(task_type);
```

### 2. 创建缺失的认证表（如果需要）
运行 `SQLSERVER_补充认证表.sql`:
- `users` 表（如果使用数据库认证）
- `otp_tokens` 表（OTP 验证码）

### 3. 创建缺失的视图
目前只有 4 个视图，Supabase 有 5 个：
- ✅ view_assessments_full
- ✅ view_department_gaps
- ✅ view_employee_gaps
- ✅ view_skill_gaps
- ❌ view_resource_planning_latest (需要从 `SQLSERVER_SCHEMA_修正.sql` 创建)

---

## 🎯 下一步工作

### 1. 完成数据库配置 ✅
- [x] 所有表数据导入
- [ ] 重建约束和索引
- [ ] 创建缺失的视图
- [ ] 创建认证相关表（可选）

### 2. 搭建 FastAPI 后端
- [ ] 创建 `backend/` 目录结构
- [ ] 实现数据库连接 (`database.py` + pyodbc)
- [ ] 实现认证模块 (`auth.py` + JWT + OTP)
- [ ] 实现 CRUD 接口（11 个表）

### 3. 前端改造
- [ ] 替换 Supabase SDK → FastAPI HTTP 请求
- [ ] 修改 `src/lib/supabaseService.ts`
- [ ] 更新认证流程（OTP + JWT）

### 4. 测试验证
- [ ] 端到端功能测试
- [ ] 数据完整性验证
- [ ] 性能测试

---

## 📚 相关文档

1. **CSV导入快速开始.md** - 使用指南
2. **SUPABASE_TO_SQLSERVER_表结构迁移完整指南.md** - 完整技术文档
3. **SUPABASE_数据导入SQL_Server操作指南.md** - 手动导入指南（已不需要）
4. **import_csv_to_sqlserver.py** - 自动导入脚本
5. **check_identity_columns.py** - IDENTITY 列检查脚本
6. **run_pre_import_fix.py** - 导入前修复脚本
7. **PRE_IMPORT_FIX.sql** / **POST_IMPORT_FIX.sql** - 修复 SQL 脚本

---

## ✨ 总结

✅ **数据迁移成功完成！**

- 从 Supabase (PostgreSQL) 成功导出 11 个表的 CSV 数据
- 通过 Python 自动化脚本批量导入到 SQL Server
- 解决了多个技术难题（日期格式、IDENTITY 列、约束冲突等）
- 数据完整性 100%：**3013 行全部导入成功**

**下一阶段**：开始 FastAPI 后端开发，实现完整的前后端集成。

---

**导入完成时间**: 2026-01-29 17:37  
**文档更新时间**: 2026-01-29 17:40
