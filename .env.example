# Supabase配置
# 请复制此文件为 .env 并填入您的实际值
# 不要将 .env 文件提交到Git仓库！

# Supabase项目URL
# 在 Supabase Dashboard → Settings → API 中找到
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase公开密钥（anon key）
# 在 Supabase Dashboard → Settings → API 中找到
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# 注意：
# - VITE_ 前缀是必需的，Vite才能在客户端访问这些变量
# - 不要在此处使用 service_role key，它权限过高，不能暴露在前端
# - anon key 是安全的，可以在前端使用
