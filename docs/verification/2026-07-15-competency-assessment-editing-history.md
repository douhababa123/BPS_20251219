# 能力评估编辑与季度历史发布验证

日期：2026-07-15  
数据库：`DCCT_BPS_Debug`

## 自动化验证

- 后端专项：30 passed（迁移、历史持久化、权限路由、既有能力评估接口）。
- 前端：12 files / 137 tests passed。
- TypeScript、目标 ESLint、生产构建、OpenSpec 严格校验均通过。

## 数据库备份与迁移

- 迁移前：当前记录 430，重复员工/技能键 0，遗留目标低于现状记录 1。
- SQL Server 已有 Veeam 完整备份：2026-07-14 20:59:09（数据库服务器时间）。
- 发布专用备份：`DCCT_BPS_Debug_pre_competency_history_20260715_121018.bak`。
- 专用备份采用 `COPY_ONLY, CHECKSUM`；`msdb` 记录大小 15.1 MiB、`has_backup_checksums=1`。
- 当前账号无 `RESTORE VERIFYONLY/CREATE DATABASE` 权限，因此使用外层事务进行完整回滚演练；迁移态为当前 430、基线 430、重复 0、无效最新记录 0，回滚后历史表不存在且原 430 条数据保持不变。
- 正式迁移连续执行两次：两次均为当前 430、基线 430、历史 430、重复 0、无效最新记录 0。
- 最终校验：`history_table=1, current_duplicates=0, invalid_current=0, invalid_web_history=0`。
- 原遗留负 GAP 完整保存在 `MIGRATION_BASELINE`，最新投影规范为目标等于现状。

## 本地浏览器与 API 验收

- 管理员矩阵：所有人员已有和缺失能力格均可编辑/录入。
- 普通工程师 `jianjun.chen@bshg.com`：仅本人 38 个能力格可编辑，其他人员可编辑格为 0。
- 卡片、表格视图：可编辑能力按钮均为 0。
- 页面颜色：红色正 GAP 单元格 106，绿色达标单元格 324，黄色 GAP 单元格 0。
- 管理员等值保存：当前记录保持 3/3，历史由 1 增至 2，新增 `WEB_EDIT` 为 2026 Q3。
- 工程师本人等值保存：历史由 2 增至 3。
- 工程师修改他人：HTTP 403，目标历史保存前后均为 1。
- 管理员密码登录缺陷已修复：启用的独立 admin 无需员工记录即可登录；普通用户仍必须绑定启用员工，停用 admin 仍被拒绝。

## 回滚准备

- 应用异常：回滚本功能提交并重新部署；兼容的历史表可保留。
- 迁移不变量异常：使用上述发布专用备份或最近 Veeam 完整恢复点恢复，禁止执行临时破坏性 SQL。

## 待发布验证

- GitHub Actions Jetson 工作流成功。
- Jetson 页面管理员/工程师复验及健康检查。
- 部署后当前记录与新增历史快照再次核对。
